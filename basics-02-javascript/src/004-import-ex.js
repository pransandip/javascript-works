/*
 * To load an ES module, set "type": "module" in
 *the package.json or use the .mjs extension.
 */

import { dividation, multiplication } from "./calc.js";

console.log(dividation(10, 2));
console.log(multiplication(10, 2));
